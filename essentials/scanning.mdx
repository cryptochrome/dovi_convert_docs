---
title: "File Scanning & Analysis"
description: "Using dovi_convert to scan and analyze your files"
icon: "scanner-image"
---

This page will be an extensive one, explaining the two file analyis features -scan and -inspect in detail, along with concepts, terminology and usage examples. 

It should have sections like this:

1. General intro to file scanning & analysis
2. Purpose and why you'd use it
3. The difference between -scan and -inspect (as explained in the dovi_convert repo README.md, but more verbose, userfriendly), e. g. "How it works"
4. Highlight that -scan is useful for an overview, but that all other commands like -convert and -inspect will actually use the same scan before they start converting (and warn when better not to convert)
5. Section about -scan
   1. Terminology used (MEL, Simple FEL, Complex FEL)
   2. Understanding -scan verdicts
   3. When to use -inspect to verify
   4. Usage examples (including all command options)
6. Section about -inspect
   1. When to use it
   2. How it's different from -scan and how it works (e. g. full frame by frame analysis)
   3. Why it's verdict can be different from -scan (-inspect being the definitive result)
   4. Interpreting the output
   5. Usage examples (including all command options)
   6. 